# Environment — never commit real secrets. Root .gitignore also covers .env / .env.local;
# these entries keep backend-local variants ignored if the root rules ever change.
.env
.env.*
!.env.example

# Local SQLite database + runtime data (created at boot/seed; not covered by root .gitignore)
data/
