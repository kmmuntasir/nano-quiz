---
description: Comprehensive PR review covering the Express/TypeScript backend and React/Tailwind frontend: architecture, correctness, security, and test coverage. Use when user requests to review a pull request or compare branches for code review.
agent: build
---

Load and execute the `pr-review` skill. If the user specified branches, pass them along; otherwise use the skill's defaults (current branch → main).

Branches (optional): $ARGUMENTS